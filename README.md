# MacOS Dotfiles

Just run the `install.sh` script after cloning the repo into ~/.config and everything should be fine
Change kitty.conf's font if you dont have it, and tmux-sessionizer's directories in `scripts/`
